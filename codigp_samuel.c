#include <stdio.h>
#include <stdlib.h>

int gera_valor(int x, int valor){
    srand(time(NULL));
    x = (rand() % valor) + 1;
    return x;
}


int main(){
    int i, valor = ;
    FILE *arq = fopen("input.txt","w");
    int vert;
    scanf("%i" , &vert);
    for(i = 0; i != valor;i++){

    }
}
